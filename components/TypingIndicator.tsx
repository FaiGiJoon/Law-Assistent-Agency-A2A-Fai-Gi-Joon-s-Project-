import React from 'react';

// This component is no longer used as the typing animation is now
// part of the Message component when content is empty.
// It is kept to prevent import errors but can be removed if all references are updated.

const TypingIndicator: React.FC = () => {
    return null;
};

export default TypingIndicator;